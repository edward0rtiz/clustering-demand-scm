# Travis CI
language: python

python:
  - "3.8"

cache:
  - pip

install:
  - pip install -r requirements.txt

services:
  - docker
  
install:
  - docker-compose build
  - docker-compose up -d 

script:
  - docker ps | grep -q kiwibot-kda
